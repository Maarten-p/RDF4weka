package Main;

/**
 * Which algorithm to use to build/use a model. Contains both associators and classifiers.
 * This might be split up if necessary.
 */
public enum Algorithm {
    FPGROWTH, APRIORI, RANDOMTREE
}
